# Database Configuration
# Choose either PostgreSQL (recommended for production) or SQLite (for development)

# PostgreSQL Connection String (if set, PostgreSQL will be used)
# DATABASE_URL=postgresql://username:password@localhost:5432/database_name
# Example for local development:
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/linera_indexer

# SQLite Path (used only if DATABASE_URL is not set)
DB_PATH=../docker/indexer-data/indexer.db
# Alternative SQLite paths you might use:
# DB_PATH=/path/to/your/indexer.db
# DB_PATH=./data/blockchain.db

# API Server Port (optional, defaults to 3002)
# EXPLORER_API_PORT=3002