// Copyright (c) Zefchain Labs, Inc.
// SPDX-License-Identifier: Apache-2.0

//! This module provides a shared key-value store server based on the RocksDB store and the in-memory store of `linera-views`.
//! The corresponding client implements the `KeyValueStore` and `KeyValueDatabase` traits.

#![deny(missing_docs)]

/// The protobuf-generated gRPC types and service definitions for the storage service.
pub mod key_value_store {
    // The types below are generated by `tonic` and cannot carry doc comments.
    #![expect(missing_docs)]
    tonic::include_proto!("key_value_store.v1");
}

/// Running the storage service as a child process.
pub mod child;
/// The client connecting to a remote storage service.
pub mod client;
/// Shared types used by both the client and the server.
pub mod common;
