apiVersion: v1
kind: Service
metadata:
  name: validator-ingress
spec:
  type: LoadBalancer
  selector:
    app: validator-1
  ports:
    - name: linera
      protocol: TCP
      port: 19100
      targetPort: linera-port
