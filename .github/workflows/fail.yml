name: Fail

on:
  push:
    branches: [ main ]
  pull_request:
    branches:
      - "**"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    steps:
    - name: Fail loudly
      run: |
        >&2 echo WORKFLOW INTENTIONALLY FAILING
        exit 1
